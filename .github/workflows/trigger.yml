name: MSBuild

on:
  pull_request:
    types: [opened, reopened]
  pull_request_review:
    types: [edited]

jobs:
  build:
    runs-on: windows-2019
    steps:
      - name: PullRequest Type
        run: write-host "PullRequest Type"
        shell: pwsh
